export type Device = {
  id: number;
  tid: number;
  name: string;
  zone_id: number;
  zone_name: string;
  region_id: number;
  region_name: string;
  is_active: boolean;
  registered_by_id: number;
  registered_by: string;
};
